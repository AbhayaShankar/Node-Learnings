const num1 = 5;

const num2 = 10;

function addValues() {
  console.log(`The sum is: ${num1 + num2}`);
}

addValues();

// we are invokign the fucntion call in the module. So while importing its not necessarily needs to be assigned...

// Check 03-module.js where we import/require this module without declaring it.
